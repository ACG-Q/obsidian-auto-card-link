/**
 * 常量定义文件
 * 
 * 此文件包含插件使用的所有常量定义
 */

/**
 * 卡片链接代码块的语言标签
 */
export const LANGUAGE_TAG = "cardlink";

/**
 * 插件ID
 */
export const PLUGIN_ID = "obsidian-auto-card-link";

/**
 * 插件名称
 */
export const PLUGIN_NAME = "Auto Card Link";

/**
 * 默认截图API参数
 */
export const DEFAULT_SCREENSHOT_PARAMS = "&dimension=1280×720";

/**
 * 默认缓存有效期（毫秒）
 * 默认为1小时
 */
export const DEFAULT_CACHE_TTL = 3600000;

/**
 * 默认请求超时时间（毫秒）
 * 默认为10秒
 */
export const DEFAULT_REQUEST_TIMEOUT = 10000;

/**
 * 默认重试次数
 */
export const DEFAULT_RETRY_COUNT = 3;

/**
 * 默认重试延迟（毫秒）
 */
export const DEFAULT_RETRY_DELAY = 1000;